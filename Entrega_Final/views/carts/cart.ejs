<% for const item of products %>
<div>
    <h3><%= item.name %></h3>
    <p><%= item.description %></p>
    <p>Precio:
    <span>$<%= item.price %></span>
    </div>
<% } %>

    <% console.log(products) %>